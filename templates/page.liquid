{%- comment -%} Template de page simple — à étoffer ultérieurement. {%- endcomment -%}
<article class="page">
  <h1>{{ page.title }}</h1>
  <div class="page__contenu">{{ page.content }}</div>
</article>
