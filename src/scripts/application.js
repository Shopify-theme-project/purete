/**
 * Script principal du thème Pureté.
 * Point d'entrée bundlé vers /assets/application.js par esbuild.
 */

// Met à jour le compteur du panier après ajout produit.
document.addEventListener('DOMContentLoaded', () => {
  // Placeholder : initialisation des modules JS du thème.
  // Ex. : menu mobile, mini-panier, validation formulaires...
  console.info('[Pureté] Thème initialisé');
});
